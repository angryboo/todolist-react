import React from 'react';
import classNames from 'classnames/bind';
import style from './Header.css';

const st = classNames.bind(style);

const Header = () => <div className={st('header')} />;

export default Header;
